import SwiftUI

enum Icons {
    static let map = Image(systemName: "map")
    static let friends = Image(systemName: "person.2")
    static let real = Image(systemName: "sparkles")
    static let profile = Image(systemName: "person.crop.circle")
}

