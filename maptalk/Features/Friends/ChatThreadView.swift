import SwiftUI

struct ChatThreadView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Chat coming soon")
                .font(.title3)
            Text("Map-integrated messaging will appear here.")
                .font(.subheadline)
                .foregroundStyle(.secondary)
        }
        .padding()
    }
}

